## End-to-End Machine Learning Pipeline


### Note
Use `python -m pip` instead of `pip` so the packages are installed into the same interpreter that runs your project.

Do not place `-e .` inside [requirements.txt](requirements.txt) for a normal dependency install. If you want to install this project itself in editable mode, run:

```powershell
python -m pip install -e .
```

